chown -R root:vhost-admin /opt/vhost-manager
chmod -R 755 /opt/vhost-manager

addgroup vhost-admin


adduser <user> vhostadmin
adduser <user> sudo

alias vhost="sudo vhost"
nano ~/.bashrc
source ~/.bashrc

echo "alias vhost=\"sudo vhost\"" >> /home/<user>/.bashrc

echo "alias vhost=\"sudo vhost\"" >> /etc/bash.bashrc


sudo nano /etc/sudoers
vhost-admin     ALL = (root) NOPASSWD: /usr/local/bin/vhost


echo "vhost-admin     ALL = (root) NOPASSWD: /usr/local/bin/vhost" >> /etc/sudoers


test ved at lave nye brugere