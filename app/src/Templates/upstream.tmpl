upstream ${upstream_name} {
  ${servers}
}

server {
  listen 80 default_server;
  listen [::]:80 default_server;
  root ${www_root};
  server_name _;

  location / {
    proxy_pass http://${upstream_name};
  }
}